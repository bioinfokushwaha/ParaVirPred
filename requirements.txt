# ──────────────────────────────────────────────────────────────────────────────
# ParaVirPred — Standalone Offline Prediction Pipeline
# Requirements File
#
# Install all dependencies with:
#     pip install -r requirements.txt
#
# Notes:
#   - PyTorch installation varies by platform and CUDA version.
#     Visit https://pytorch.org/get-started/locally/ for the exact command.
#   - fair-esm (Meta's ESM2) must be installed separately (see README).
#   - Only the packages needed for your chosen embedding variant are actually
#     used at runtime — lazy imports keep startup fast.
# ──────────────────────────────────────────────────────────────────────────────

# Core scientific stack
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
openpyxl>=3.1.0          # required for Excel (.xlsx) output

# Biopython — physicochemical feature computation (ProtParam)
biopython>=1.81

# PyTorch — required for all DNN (.pth) models and embedding models
# Install the correct build for your system from https://pytorch.org/get-started/locally/
# Example (CPU-only):
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
# Example (CUDA 12.1):
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# HuggingFace Transformers — required for ProtT5 embedding variants
transformers>=4.35.0
sentencepiece>=0.1.99     # ProtT5 tokenizer dependency

# fair-esm (Meta ESM2) — required for ESM2 embedding variants
# Install with:
#     pip install fair-esm
# or from source:
#     pip install git+https://github.com/facebookresearch/esm.git
fair-esm>=2.0.0

# XGBoost — required if using XGBoost .pkl models
xgboost>=2.0.0
